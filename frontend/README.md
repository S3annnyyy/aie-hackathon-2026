# Interior Frontend Test

React + TypeScript + Vite UI for PDF-to-3D HDB interior MVP.

## Run

```bash
cd interior-frontend-test
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if backend is not `http://localhost:4190`.
